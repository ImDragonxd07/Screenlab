name: Update Code Stats

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  stats:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install tokei
        run: |
          wget https://github.com/XAMPPRocky/tokei/releases/download/v13.0.0-alpha.5/tokei-x86_64-unknown-linux-gnu.tar.gz
          tar xzf tokei-x86_64-unknown-linux-gnu.tar.gz
          sudo mv tokei /usr/local/bin/
          tokei --version

      - name: Generate stats
        run: |
          tokei --output json > tokei.json
          
          FILES=$(jq '[.[] | .reports | length] | add // 0' tokei.json)
          CODE=$(jq '[.[] | .code] | add // 0' tokei.json)
          
          cat > stats.txt << EOF
          ## 📊 Project Stats
          - Files: ${FILES}
          - Lines of Code: ${CODE}
          EOF
          
          awk '
            /<!-- STATS_START -->/ {print; system("cat stats.txt"); f=1; next}
            /<!-- STATS_END -->/ {f=0}
            !f
          ' readme.md > readme.tmp
          mv readme.tmp readme.md

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add readme.md
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update code stats [skip ci]"
            git push
          fi